specification natlist1
begin

  properties
  begin
    ordering 
      precedence perm member, perm delete
      status perm : l   
    theorems
      [T1] delete(x, nl1) == nl1
           when not(member(x, nl1)) == true ;

  end
end
